apiVersion: apps/v1
kind: Deployment
metadata:
  name: weather-mcp-server
  labels:
    app: weather-mcp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: weather-mcp
  template:
    metadata:
      labels:
        app: weather-mcp
    spec:
      containers:
        - name: weather-mcp
          image: ghcr.io/huang195/mcp:latest
          imagePullPolicy: Always
          command: ["sh", "-c"]
          args:
            - uv run /app/mcp-server/weather/weather.py
          ports:
            - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: weather-mcp-service
spec:
  selector:
    app: weather-mcp
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
